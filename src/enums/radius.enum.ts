/**
 * Border radius enum.
 *
 * Defines the available border radius options for UI elements.
 *
 * @enum {string}
 */
export enum RadiusEnum {
    XS = "xs",
    SM = "sm",
    MD = "md",
    LG = "lg",
    XL = "xl",
}
