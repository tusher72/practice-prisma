/**
 * Environment types enum.
 *
 * Defines the valid environment values for the application.
 * Used for type-safe environment checks throughout the codebase.
 *
 * @enum {string}
 */
export enum EnvironmentEnum {
    DEVELOPMENT = "development",
    PRODUCTION = "production",
    TEST = "test",
}
